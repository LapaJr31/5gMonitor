logger:
    file:
      path: /var/log/open5gs/pcf.log

sbi:
    server:
      no_tls: true
    client:
      no_tls: true

global:
  max:
    ue: MAX_NUM_UE

pcf:
    sbi:
      server:
        - address: PCF_IP
          port: 7777
      client:
        scp:
          - uri: http://SCP_IP:7777
    metrics:
      server:
        - address: PCF_IP
          port: 9090

db_uri: mongodb://MONGO_IP/open5gs
